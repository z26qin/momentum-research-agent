# DeepSeek (OpenAI-compatible). Required.
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com

# Optional overrides. Official aliases deepseek-chat / deepseek-reasoner
# were retired in July 2026; if calls fail, set:
#   SUB_AGENT_MODEL=deepseek-v4-flash
#   COORDINATOR_MODEL=deepseek-v4-pro
SUB_AGENT_MODEL=deepseek-chat
COORDINATOR_MODEL=deepseek-reasoner

# Web search — set at least one. Serper is tried first, then Tavily.
SERPER_API_KEY=
TAVILY_API_KEY=
